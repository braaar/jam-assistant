import { Box, Container, Heading } from "theme-ui";

export const Header = () => (
  <Box sx={{ background: "secondary" }}>
    <Container>
      <Heading>Jam assistant</Heading>
    </Container>
  </Box>
);
